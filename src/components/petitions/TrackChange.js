import React from 'react'

function TrackChange() {
  return(
    <section id="petition" className="card container">
      /* form that on submit changes the json/firebase data for a particular student to a different track */
      <form id="trackChange">
        <ul>
          <li></li>
          <li></li>
        </ul>
      </form>
    </section>
  )
}

export default TrackChange
